//JavaScript variables const is a keyword used to declare a variable that cannot be reassigned a new value. It is similar to let , but the value of a const variable cannot be changed once it has been declared.
const intrestRate = 0.3;
console.log(intrestRate);