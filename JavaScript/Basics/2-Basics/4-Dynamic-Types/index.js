//program Languages
// - Static languages- in static languages when we declare a variable, the type of the variable is set and it connot be changed in the future
// - Dynamic languages - in a dynamic language like JS the type of a variable can change at run time.

let name = 'sam';
let age = 20;
let isApproved = true;
let firstName = undefined;
let lastName = null; 

console.log(typeof(name));

name = 10;
console.log(typeof(name));
