// Joining Array

const numebrs = [1, 2, 3, 4];
// join use to join array into string
const joined = numebrs.join(',');
console.log(joined);

const message = 'This is my first message';
// split method use to split string into array
const parts = message.split(' ');
console.log(parts);