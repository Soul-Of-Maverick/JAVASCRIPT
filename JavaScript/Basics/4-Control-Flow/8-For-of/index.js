// For-of loop
// for -of loop to itirate over the elements ot items in an array.
// new ways to itirate over a raise

let colors = ['red', 'green', 'blue'];

for (let color of colors) {
    console.log(color);
}
